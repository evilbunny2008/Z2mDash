- Added Android TV support: appears on the TV's own launcher, full remote D-pad navigation throughout the app, and a side navigation rail in place of the phone's bottom bar
- Added a "Permit Join" toggle, on each broker's edit screen and right on the Home screen, to open your Zigbee network to new devices - with router/coordinator suggestions and a live countdown
- Added a "Tile & Cluster Width" slider in Settings to resize tiles, and a "Blink Warnings" setting for out-of-range sensor alerts
- Added "Import via code" for securely receiving a broker's settings, and an "Auto-Accept Discovered Devices" setting per broker
- Fixed reordering clusters/panels not syncing reliably when running the app on more than one phone against the same broker
- Fixed deleting a broker leaving orphaned panels behind, and added a "Clean Up Orphaned Data" option for any left over from before this fix
- Fixed "Background Work" (off) not actually stopping broker connections, and added a check on resume that reconnects if a connection quietly died in the background
- Fixed a sensor tile's value getting cut off on narrow tiles, and a cluster's 3rd column clipping its tile content
- Performance: faster cold start, and tiles no longer redraw on every unrelated incoming MQTT message
