- Sensor tiles that hold a fixed preference rather than a live reading (e.g. a soil moisture min/max threshold) can now be marked "Editable value" in their panel edit screen - tapping the tile then lets you type a new number, which republishes to that tile's own topic/field (including the device's "/app" config, when that's where the value lives), so it survives future reconciliation.
